---
sidebar_position: 4
---
import BrowserOnly from '@docusaurus/BrowserOnly';

# Paragraph

`@edybara-editor/paragraph` 에디터에 **문단**을 표현하기 위한 서식과 관련 플러그인을 제공합니다.

## 설치

```bash
npm install @edybara-editor/paragraph
```

## 스타일

```css title="src/index.scss"
@import "@edybara-editor/paragraph/styles/paragraph.scss";
```

## 서식

#### `edybaraParagraphNodes`

문단 서식을 제공합니다.

## 플러그인

#### `edybaraParagraphPlugins`

하단의 모든 플러그인을 통합 제공합니다.

#### `edybaraBasicKeymapPlugins`

현재 선택 영역의 블록 노드를 Paragraph 노드로 전환하는 단축키를 제공합니다.

## 사용 예시

```typescript title="src/index.ts"
import { EditorState, Plugin } from 'prosemirror-state';
import { Schema } from 'prosemirror-model';
import { EditorView } from 'prosemirror-view';
import { edybaraBaseNodes, edybaraCorePlugins, mac } from '@edybara-editor/core';
import { edybaraMenubarPlugins } from '@edybara-editor/menubar';
import { edybaraParagraphNodes, edybaraParagraphPlugins, } from '@edybara-editor/paragraph';

const schema = new Schema({
  nodes: {
    ...edybaraBaseNodes(),
    ...edybaraParagraphNodes({
      allowAlign: true,
      nodeName: 'paragraph',
    }),
  },
});

const plugins: Plugin[] = [
  ...edybaraParagraphPlugins({
    nodeType: schema.nodes['paragraph'],
  }),
  ...edybaraMenubarPlugins({
    align: {},
  }),
  ...edybaraCorePlugins(),
];

const view = new EditorView(document.querySelector('#editor'), {
  state: EditorState.create({
    schema: schema,
    plugins: plugins,
  }),
});
```

<BrowserOnly>
  {() => {
    const { ParagraphExample } = require('@site/src/components/examples/packages/paragraph.tsx');
    return <ParagraphExample />
  }}
</BrowserOnly>

